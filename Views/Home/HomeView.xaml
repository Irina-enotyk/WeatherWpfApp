<UserControl x:Class="WeatherWpfApp.Views.Home.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeatherWpfApp.Views.Home"
             xmlns:vm="clr-namespace:WeatherWpfApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             FontFamily="{StaticResource MainFont}">
    <Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <Grid 
                    Grid.Column="0"
                    Margin="0, 10, 0, 10">
                    <local:DayForecastDetailsView DataContext="{Binding SelectedDay}"/>
                </Grid>
                <Grid Grid.Column="1"
                      Margin="10">
                    <local:HourlyForecastDetailsView DataContext="{Binding SelectedDay}"/>
                </Grid>
            </Grid>

            <Grid
                Grid.Row ="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button 
                    x:Name="ScrollToLeft_Button"
                    Grid.Column="0"
                    Margin="10">
                </Button>

                <ScrollViewer
                    x:Name="WeatherDays_ScrollViewer"
                    Grid.Column="1"
                    Margin="5"
                    ScrollViewer.HorizontalScrollBarVisibility ="Hidden"
                    ScrollViewer.VerticalScrollBarVisibility ="Disabled">
                    
                    <ListBox 
                        x:Name="WeatherDays_ListBox" 
                        Grid.Row="1" 
                        Background="{DynamicResource LightBackground}"
                        Margin="5"
                        ItemsSource="{Binding ForecastDays}"
                        SelectedItem="{Binding SelectedDay}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column ="0">
                                            <Grid >
                                                <Grid.RowDefinitions>
                                                    <RowDefinition/>
                                                    <RowDefinition/>
                                                </Grid.RowDefinitions>
                                                <Grid Grid.Row="0">
                                                    <Label Content="{Binding Path=Date}"/>
                                                </Grid>
                                                <Grid Grid.Row="1">
                                                    <Label Content="{Binding Path=WeekDay}"/>
                                                </Grid>
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0">
                                            <Label Content="{Binding Path=MaxTemperature}"/>
                                        </Grid>
                                        <Grid Grid.Column="1">
                                            <Label Content="{Binding Path=MinTemperature}"/>
                                        </Grid>
                                    </Grid>
                                    <Label Content="{Binding Path=Weather}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>

                <Button 
                    x:Name="ScrollToRight_Button"
                    Grid.Column="2"
                    Margin="10">
                </Button>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
